warning: in the working copy of 'Backend/server.js', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/Backend/server.js b/Backend/server.js[m
[1mindex 77a8d6b..a900e54 100644[m
[1m--- a/Backend/server.js[m
[1m+++ b/Backend/server.js[m
[36m@@ -1,18 +1,56 @@[m
 require('dotenv').config();[m
 const http = require('http');[m
[31m-const app = require('./app');[m
[31m-const { initializeSocket } = require('./socket');[m
[31m-const { getAllowedOriginsList } = require('./config/cors.config');[m
[32m+[m[32mconst app = require('./src/app');[m
[32m+[m[32mconst { ensureDbConnected, disconnectDb } = require('./src/config/db');[m
[32m+[m[32mconst { initializeSocket } = require('./src/socket');[m
[32m+[m[32mconst { getAllowedOriginsList } = require('./src/config/cors.config');[m
 [m
 const port = process.env.PORT || 5001;[m
 [m
[32m+[m[32m/**[m
[32m+[m[32m * Mounts (see app.js): /users (POST /login, /register, …), /captains (POST /login, …),[m
[32m+[m[32m * /maps, /rides, /admin, /driver-subscriptions, /health, POST /webhooks/razorpay, Socket.IO on same server.[m
[32m+[m[32m */[m
[32m+[m
 const server = http.createServer(app);[m
 initializeSocket(server, app);[m
 [m
[31m-server.listen(port, () => {[m
[31m-    console.log(`Server is running on port ${port}`);[m
[31m-    console.log(`[CORS] NODE_ENV=${process.env.NODE_ENV || 'development'}`);[m
[31m-    if (process.env.NODE_ENV === 'production' || process.env.RENDER === 'true') {[m
[31m-        console.log('[CORS] Socket.IO + REST allow origins:', getAllowedOriginsList());[m
[32m+[m[32mfunction gracefulShutdown(signal) {[m
[32m+[m[32m    console.log(`[shutdown] ${signal}`);[m
[32m+[m[32m    server.close(async () => {[m
[32m+[m[32m        try {[m
[32m+[m[32m            await disconnectDb();[m
[32m+[m[32m        } catch (e) {[m
[32m+[m[32m            console.error('[shutdown] Mongo close error:', e.message);[m
[32m+[m[32m        }[m
[32m+[m[32m        process.exit(0);[m
[32m+[m[32m    });[m
[32m+[m[32m    setTimeout(() => {[m
[32m+[m[32m        console.error('[shutdown] force exit after timeout');[m
[32m+[m[32m        process.exit(1);[m
[32m+[m[32m    }, 10_000).unref();[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32mprocess.on('SIGTERM', () => gracefulShutdown('SIGTERM'));[m
[32m+[m[32mprocess.on('SIGINT', () => gracefulShutdown('SIGINT'));[m
[32m+[m
[32m+[m[32masync function start() {[m
[32m+[m[32m    try {[m
[32m+[m[32m        await ensureDbConnected();[m
[32m+[m[32m    } catch (err) {[m
[32m+[m[32m        console.error('[server] MongoDB required to start:', err?.message || err);[m
[32m+[m[32m        process.exit(1);[m
     }[m
[31m-});[m
[32m+[m
[32m+[m[32m    server.listen(port, () => {[m
[32m+[m[32m        const base = `http://localhost:${port}`;[m
[32m+[m[32m        console.log(`REST + Socket.IO: ${base}`);[m
[32m+[m[32m        console.log(`Server is running on port ${port}`);[m
[32m+[m[32m        console.log(`[CORS] NODE_ENV=${process.env.NODE_ENV || 'development'}`);[m
[32m+[m[32m        if (process.env.NODE_ENV === 'production' || process.env.RENDER === 'true') {[m
[32m+[m[32m            console.log('[CORS] Socket.IO + REST allow origins:', getAllowedOriginsList());[m
[32m+[m[32m        }[m
[32m+[m[32m    });[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32mstart();[m
