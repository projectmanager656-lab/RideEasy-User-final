- Prefers modifying the existing project rather than creating a new one; inspects the existing code first and preserves currently working functionality. Confidence: 0.9
- When integrating new requirements into an already-working app, expects a strict read-only inspection phase first: no file modifications/creations/deletions/renames, no package installs, no dependency or route changes, and no redesign — the agent must deliver an inspection report and wait for the user's explicit approval before writing any code. The user states this emphatically and explicitly ("Do not modify any files", "Do not modify code, UI, backend, database, or configuration. I only want an inspection report and testing recommendation"). Confidence: 0.98
- Prefers verifying that referenced asset files exist before editing, and reusing the project's actual folder structure (e.g. `assets` vs `assest`) rather than creating duplicate folders or assets. Confidence: 0.7
- When a surrounding feature is purely cosmetic and already backed by demo/mock data (e.g. nearby-vehicle markers around the pickup during driver search), keeps the existing mock positions and swaps only the marker visuals — explicitly forbids creating a new backend/API, fake driver accounts, or driver-connectivity logic to make the mock data "real". Confidence: 0.8
- Prefers preserving backend/API integration and avoids changing API endpoints unless absolutely necessary. Confidence: 0.9
- Verifies changes by running the application (e.g., `npm run dev`) and checking for errors before reporting completion. Confidence: 0.8
- Avoids destructive git commands (e.g. `git reset --hard`, `git checkout`, repo-wide resets); prefers keeping existing working code and git changes intact rather than resetting. Confidence: 0.85
- Avoids disrupting running processes; will not kill `node.exe` or stop an already-running dev server unnecessarily. Confidence: 0.8
- Prefers minimal, targeted changes: when asked to swap one element (e.g., an image), leaves all other elements (e.g., text/copy) exactly as-is unless explicitly asked to change them. The user will explicitly say "Do NOT redesign / keep it exactly the same / do not change any other part of the UI / keep everything as it is and just make X" to enforce surgical scope on a single element — including "okey all good, keep everything as it is and now just remove X", where X is described by its visible content/labels (e.g. "the vertical lines which contains Finding driver → Assigned → Arrived → Live ride → Done"). Confidence: 0.97
- When the user rejects an unrequested addition or over-delivered change (e.g., a newly added search bar), expects it fully reverted to restore the original UI/layout exactly, including deleting the unused component, removing its import/handlers from JSX, and collapsing shared helper logic back to inline — not simply hidden. Confidence: 0.9
- When the user supplies their own asset file (with a specific path), uses that asset directly (swap the import/reference) and adjusts only the framing to suit it, rather than re-tuning the old asset or recreating the asset. This includes the user iterating on which specific asset file should be used (e.g. swapping `logo-premium.png` for `premium.png`, or pointing to a named path like `src/assets/images-removebg-preview.png` to replace a marker image), expecting the referenced asset swapped cleanly with no other changes. Confidence: 0.85
- When the user asks to "completely remove" a feature/option, expects clean, thorough removal: delete now-unused component files and imports/handlers, leave no leftover references or UI traces anywhere, and reflow/relocate remaining content naturally so there is no empty gap or broken layout. Confidence: 0.85
- When a detailed requirement leaves the behavior ambiguous (e.g. whether a new selectable drop-address list is purely visual or should be wired into the normal booking flow), asks a clarifying multiple-choice question and proceeds to implement only after the user picks the intended behavior — the user engages with these options and expects the change to be vetted against the existing flow before coding. Confidence: 0.7
- For new feature work, expects the complete interaction flow implemented end to end (full UI interaction plus wiring to existing backend/storage), not just static UI — the user explicitly says "Do not stop after creating static UI. Implement the complete frontend interaction flow and connect to existing backend APIs wherever they are already available." Confidence: 0.85
- The user frequently changes their mind shortly after a UI change is implemented and asks to revert it ("keep my home page like before / remove all contents that you made in the last prompt"): expects a clean, complete rollback of only that change — restoring the component to its prior state, deleting the added handler/prop/imports, and confirming lint and diagnostics are clean — without hesitation or re-proposing the change. This oscillating behavior runs both ways: the user may later ask to RESTORE a feature (e.g. a typing suggestion dropdown) that was removed at their own request earlier, expecting it brought back fully re-wired into the existing flow (restored state/handlers/panel render), not partially or merely hidden. Confidence: 0.8
- When wiring a new screen into an existing flow, expects the underlying backend call/side effect to happen exactly once — explicitly forbids duplicate calls ("Do NOT create a second ride. Do NOT call the ride creation API twice") and forbids replacing the existing API implementation; the new screen must consume the existing endpoint rather than duplicating or swapping it. Confidence: 0.85
- When a new screen is added to the app, expects the existing page to hand off all relevant state (ride id + details) via the app's existing navigation/persistence convention (e.g. route state + the established session-storage key) so the new page can pick up monitoring without re-creating anything. Confidence: 0.75
- After finishing a scoped task, expects the agent to run the existing lint/build check if available, then STOP — with no additional improvements or unrelated fixes beyond exactly what was requested. Confidence: 0.85
- When adding new visual behavior that only one screen needs, extends the shared component with an optional prop defaulted to the current behavior (e.g. `nearbyVehicles = []` on RideMap) so every other consumer renders exactly as before, rather than changing the shared component's default output. Confidence: 0.8
- When a feature behavior causes an unwanted side effect (e.g. the started → /riding auto-redirect hijacking tab navigation), prefers preserving the intended feature by gating it on existing flow-state (e.g. `keepSearchFirstRef`, which is only false while the user is actively in the booking flow) instead of deleting the transition outright — explicitly approves the "safer minimal fix" that conditions the behavior on being in the current booking/ride flow, and forbids clearing ride state from the DB or session as a workaround. Confidence: 0.9
- Before applying a fix that relies on an existing flow-state flag/ref as a gate, expects the agent to verify that flag's semantics in code (where it is initialized and set, and that sibling branches already use it the same way) and confirm it is the correct condition before editing. Confidence: 0.8
- When a new feature must produce a user-facing document/summary from an existing entity (e.g. a ride invoice), expects it built from the actual data fields already present on that entity (ride doc: price, discountAmount, platformFee, chargedAmount, paymentMethod, paymentStatus, completedAt, duration, distance, populated captain object) with an explicit field-mapping shown in the plan — and any genuinely missing pieces (e.g. invoice number, tax/GST fields) flagged as needing to be added, never silently fabricated or assumed. Confidence: 0.8
- For financial/state-changing backend operations (e.g. payments, amounts, ride fares), expects the backend to validate all amounts and payment logic server-side rather than trusting values sent by the frontend, and to use appropriate persisted fields (e.g. advanceAmount, advancePaymentStatus, remainingAmount, remainingPaymentStatus) only if equivalents don't already exist — never duplicating existing payment fields. Confidence: 0.8
- For any new backend endpoint that returns user-scoped data, expects strict ownership verification on top of the existing auth middleware (e.g. the requesting user must own the ride, 403 otherwise), only the minimum fields returned, and no sensitive internals (OTP hashes, wallet/bank details, earning breakdowns) — a passenger must never fetch another user's ride/invoice by tampering with an ID. Confidence: 0.85
- Prefers persisting derived stable identifiers (e.g. an invoice number) on the existing record rather than creating a separate DB collection unless genuinely necessary, and generating the identifier exactly once so re-opening the same record always yields the same value — never a regenerated/different one per view. Confidence: 0.8
- For financial documents (e.g. ride invoices), will NOT add legal/tax line items (GST etc.) that the existing backend does not actually compute — inventing charges just because a generic template includes them is explicitly forbidden; use only the actual fare/discount/service-fee/charged amounts. Confidence: 0.85

- When asked to gate a new feature step (e.g. a 25% advance payment) within an existing ride flow, prefers a non-blocking UI-prompt approach that leaves the existing status/start logic entirely untouched — explicitly choosing "non-blocking UI only" over a "hard gate on start" option that could reject/block existing rides. The advance is prompted in the UI and recorded/persisted on the backend, but it never gates ride-start or driver flow. Confidence: 0.6

- When adding a new payment increment (e.g. 25% advance vs the existing full-fare completion payment), prefers extending the single existing mock payment endpoint with an optional discriminator field (e.g. `part: 'advance' | 'remaining'`) rather than creating a second payment route, so all charges flow through the same idempotent endpoint and the paid totals always reconcile to 100% of the fare with no double-charge. Confidence: 0.6

- When a bug's source code looks correct, the agent should verify the ground truth of the running system before concluding — checking whether the local backend process is running stale code, probing the deployed API directly (e.g. curl the route), and checking git branches/commits/deployment config to determine which code is actually live. Confidence: 0.8
- After over-engineering a minimal fix (adding state flags, extra props, conditional UI that turn out to be dead code because a route unmounts), expects the agent to recognize and revert the excess, leaving only the true minimum diff (e.g. a one-line scrollbar-hide change once the page itself is unchanged). Confidence: 0.8
- For a user-facing UI feature, expects verification in a REAL rendered browser (launch the app and inspect the actual DOM/visual output, and where applicable interact with the component — e.g. clicking the new button and confirming it flips to a success state) rather than stopping at static code edits, lint/build, or unit tests. When the user reports/provides evidence that the UI "was not rendered" ("the previous advance-payment UI was not rendered", "The current Card 3 only shows Auto and ₹424"), they expect the root cause of the dead/hidden UI traced and fixed, then the rendered page confirmed to actually display it — explicitly "verify the actual browser UI" and "Do not finish by only modifying backend code or creating unused components." Confidence: 0.75
- When a new required feature step replaces an old behavior, expects the new UI not to be gated/suppressed by the legacy setting it replaces (e.g. the 25% advance payment must appear for every booked ride regardless of the old Cash payment selection, "because the new requirement replaces that old payment behavior") — and any such gate added on the backend too must be removed so the new step works uniformly. Confidence: 0.65
