- Wants changes actually implemented, not just described; expects a follow-up report listing files changed, what changed in each, any dependency added, how to run, and whether existing flows were preserved ("tell me exactly which files were modified, and stop"). For bug-fix implementations the report must also state the exact old condition vs new condition, why the fix prevents the reported problem, and explicit confirmation that the legitimate flow still works (e.g. Searching → Started → /riding). Confidence: 0.95
- Communicates via long, heavily prescriptive checklist-style requirements with explicit constraints ("keep the existing theme/design consistent", "Do NOT redesign unrelated components", "only change X", "completely remove Y", "make sure ... work responsively") and expects every listed item to be honored exactly, with no unrequested additions or expansions. Confidence: 0.9
- Expects pre-implementation inspection reports structured as a requirements-to-files mapping: for each requirement, which existing file/component to modify, which can be implemented by reusing existing components, plus a concise list of reusable building blocks — not exhaustive prose. Confidence: 0.85
- For inspection requests, expects a numbered question-by-question report that answers every listed item explicitly, including precise endpoint/event names, DB fields and enums, storage location, and data flow, plus a concrete "safest way to test" recommendation that avoids production-code changes. Confidence: 0.88
- For diagnosing unexpected behavior (e.g. navigation being hijacked and redirected back to /riding), expects a root-cause report that names the exact triggering condition, the specific file and code path (with line references), which statuses/state are considered "active", and an explicit statement of whether the behavior is intentional — then a set of fix options ranked from least to most invasive with trade-offs and a clear recommendation, still with no code changes until approval. Confidence: 0.9
- When the user supplies their own empirical evidence (e.g. verifying in the browser console that `sessionStorage` is empty), expects the agent to treat it as authoritative for ruling out hypotheses — drop the ruled-out explanation and re-investigate (including backend socket join/catch-up paths) until the exact source is pinned down, rather than re-asserting the earlier conclusion. Confidence: 0.8
- For root-cause requests, expects an exhaustive codebase-wide sweep of every occurrence of the relevant patterns (all `navigate('/riding')` variants, `<Navigate`, storage keys, etc.) and a definitive verdict on which file/component is responsible — not just a read of the few files the user listed. Confidence: 0.75
