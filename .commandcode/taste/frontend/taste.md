- Prefers custom, polished UI components over native browser controls (avoids native date/time pickers). Confidence: 0.9
- Prefers compact grid/dropdown selectors over long scrolling lists, wheel pickers, or up/down arrow controls; values one-tap selection. Confidence: 0.9
- Mobile-first responsive design; components must work at narrow widths (360–412px) and desktop preview, with no horizontal overflow or clipped content. Confidence: 0.9
- Visual identity: dark/black background, yellow primary accent, white primary text, gray secondary text, subtle borders, rounded cards, consistent spacing. Confidence: 0.9
- For RideEasy branding, "Ride" should be white and "Easy" yellow. Confidence: 0.8
- Avoids over-engineering: prefers simple React components and existing CSS over adding large UI libraries or unnecessary dependencies. Confidence: 0.8
- Prefers reusing existing components, state, handlers, and logic over rewriting or creating new components when a working implementation already exists. Confidence: 0.9
- New modal/sheet components should match the sizing and styling conventions of the app's existing analogous components (e.g. a "For me" bottom sheet should be resized to the same compact centered box as the existing ScheduleModal, not a taller/full-width sheet) so all similar dialogs read consistently. Confidence: 0.75
- Prefers promotional card images integrated as a background layer (object-cover, positioned/cropped, blended via mask gradients) so image and text read as one unified banner — not a small standalone floating <img> box; the subject must stay recognizable, undistorted, and clear of text. Confidence: 0.9
- When a tall/wide asset's subject occupies only a small portion, prefers tuning the background layer's `objectPosition` (e.g. `right 85%`) so the complete subject is framed and fully visible — wheels/body sitting naturally on the road/ground line rather than cut off by the crop-clip; subject must not be clipped by `overflow-hidden`, and positioning must hold across desktop/tablet/mobile. Confidence: 0.85
- Prefers the banner's vehicle illustration kept small and subtle (matching the original scale, e.g. a narrower image-layer width like `w-[42%] max-w-[170px]`) rather than large/dominant — after a fix made it bigger, asked to "make that auto small like earlier / keep it like before". Confidence: 0.7
- Cares about exact user-facing copy: punctuation and capitalization in UI text must match the requested wording precisely (comma vs period, title case, no trailing period) — e.g. "Safe rides, Verified drivers." and "RideEasy Serves Kolhapur Region". Confidence: 0.7
- Wants image assets (often AI-generated) cleaned at the file level: no baked-in text, watermarks, or AI-tool symbols (e.g. the Gemini sparkle icon) may show through in the UI — only the subject (e.g. the auto-rickshaw) should remain in the image itself. Removal must be complete: ghost shadows, partial glyphs, or dimmed remnants of removed text/icons will be re-reported in follow-ups ("still showing … properly delete"). Confidence: 0.95
- Scrutinizes the final rendered card/image closely (borders, corners, edges): any artifact left by the cleanup itself — flat black voids, smudges, seams, or a broken border corner — counts as a flaw and must be repaired so the area looks natural (e.g. patching in real road texture sampled from the surroundings rather than leaving a flat fill). Confidence: 0.75
- Prefers real data over fabricated placeholder/dummy data in the UI: populates preset/quick-select lists from the app's actual dataset (e.g. real coordinates/addresses pulled from an existing service-area constants file, "no fake data") rather than inventing values, and shows honest empty states ("no recent searches") where a list has no history. Confidence: 0.8
- When a UI banner/element is meant to enable a platform capability (e.g., tapping a location banner to enable sharing), prefers a two-step flow: tapping the banner first opens an in-app consent popup with the explanatory text and explicit confirm/dismiss buttons, and only on confirm (e.g. "Turn on") is the real native/system prompt (device location + location accuracy) triggered; dismiss (e.g. "No, thanks") just closes the popup. Clicking either button must close the popup. Confidence: 0.8
- For consent/permission dialogs, prefers an in-app modal that reuses the app's existing modal styling conventions (dark rounded card matching current theme, a handle bar, title + explanatory body paragraphs, and stacked full-width buttons where the primary/confirm is the yellow accent with dark text and the secondary is the bordered card button) rather than a plain browser confirm or an unwrapped popup. Confidence: 0.75
- Converts long vertical lists into horizontally scrollable rows of cards when requested for a mobile screen, keeping the existing styling, icons, navigation, and hidden-scrollbar edge-bleed pattern. Confidence: 0.75
- For grouped promotional banner images on a mobile screen, prefers a horizontally scrollable row (left/right) over a vertical swipe stack — with the scrollbar hidden (so scrolling stays clean with no visible scrollbar menu), visible `gap` spacing between cards so they read as distinct/interactive, and a partial-card peek (e.g. `w-[72%]`) so more content is discoverable. Confidence: 0.75
- Prefers compact overall UI spacing — not just selectors: cards/modals should be made smaller (reduced width/padding, smaller buttons/option cells) and vertical gaps between stacked sections/cards kept tight but still with a small non-touching margin for a dense layout. Confidence: 0.75
- Prefers the horizontal three-dot ellipsis Remix icon (`ri-more-line`) for a "More" menu / bottom-nav item. Confidence: 0.7
- For vehicle/ride-type symbols, prefers the actual branded product logo images (e.g. `logo-auto.png` for Auto, `logo-car.png` for RideEasy Go, `logo-premium.png` for Premier, `logo-bike.png` for Bike) rather than generic icon-font glyphs — displaying the provided asset directly and introducing any new facility's own logo. Confidence: 0.65
- For fields whose value is fixed or derived automatically (e.g. the current year in a scheduling picker), prefers making them non-editable/static — displaying the value with a lock indicator instead of an interactive dropdown/selector the user could change to an invalid future value. Confidence: 0.8
- Requires polished loading and error states for async/network features (e.g. route/map, fare): a clean busy/loading indicator rather than a blank/broken area, and user-friendly error copy (e.g. "Unable to load route. Please try again.") with an explicit Retry action — never expose raw API errors and never crash the page; the error state must offer a recovery path. Confidence: 0.8
- Prefers a single, stable, locked primary CTA label (e.g. the main ride-action button always reads "Choose Ride" constant, regardless of which vehicle option is selected) rather than dynamic per-selection button text. Confidence: 0.75
- When reordering a list of ride/vehicle options (e.g. moving Bike to first position because it was last), wants the first-listed item to be the one selected/highlighted by default so the visible order and the default state match. Confidence: 0.7
- For features whose backend API does not yet exist (e.g. emergency/SOS, emergency contacts, safety preferences), expects the full frontend flow implemented with a clearly marked integration point for the real API/service, while explicitly forbidding fabricated backend responses, fake driver/ride/payment data, or invented support phone numbers. Confidence: 0.85
- When backend endpoints DO exist (e.g. ride history, support tickets), expects them reused/connected rather than building standalone static UI, and expects any existing auth/client allow-list (e.g. the frontend API client's allowed passenger endpoints) extended to permit the new endpoints. Confidence: 0.8
- Prefers whole-page (vertical) scrolling rather than a clamped/fixed-height layout, so the user can see the entire page efficiently — applied repeatedly to make all content scroll naturally into view. The page scrollbar must always be hidden (scrollbar-width: none / ::-webkit-scrollbar: none), and any overlay/modal must use `position: fixed` (not `absolute`) so it stays centered in the viewport even after scrolling. Confidence: 0.8
- Prefers the pickup (green) and drop (red) dots in a location selector to be connected by a vertical dotted line (e.g. `border-l-2 border-dotted border-zinc-400` stretching between them), so the pick→drop relationship reads as a connected route; wants this applied consistently to every rendered location selector (e.g. repeats the request for the home page "Book A Ride" card). Confidence: 0.8
- Keeps informational/support pages concise: when a section (e.g. the "Frequently Asked Questions" accordion) makes a page "too long", wants it moved out of the main page into its own dedicated page/route, accessible from the "More" menu grouped alongside existing items like Safety and Help — so the primary page stays short and secondary content is reachable from the More options. Confidence: 0.8
- For contextual/suggestion UI (e.g. live location search while typing in pickup/drop), prefers IN-PLACE content swapping over creating a new container: the suggestions must REPLACE the existing static section (e.g. the "Recent Searches" list) in the exact same area/position inside the card, instead of rendering a second dropdown/floating panel below the card that pushes the page down or jumps its height. The suggestions area should use the same horizontal boundaries and scroll internally with a fully hidden scrollbar (`scrollbar-width: none` + `::-webkit-scrollbar { display: none }`) when long, and no-match should show a small inline empty message (e.g. "No locations found") in that same spot. Confidence: 0.8
