# Taste
- Wants changes limited to the components/files directly responsible for the requested fix; explicitly forbids refactoring unrelated files or touching unrelated subsystems. Confidence: 0.95
- States explicit "do not modify" boundaries per task (e.g. other apps in the monorepo, notifications, invoices, payments, OTP) and expects them honored literally — "keep everything else exactly as it is". Confidence: 0.9
- Prefers finding and extending the existing implementation over creating a parallel system (e.g. reuse the existing map/marker code rather than introducing a second map stack). Confidence: 0.9
- Prefers investigating the current code/assets first (read the actual asset, the real call sites) and confirming root cause before editing. Confidence: 0.85
- No fake or simulated data in production code: use the authoritative backend/socket data; never invent driver movement, random coordinates, or fabricated payloads. Confidence: 0.9
- Backend validation stays authoritative; the client may mirror it but must never silently alter the user's selection (no silent conversion to another flow/time, no silent rounding) — instead surface a validation message and let the user pick a valid value. Confidence: 0.9
- Avoid manual timezone offset arithmetic; keep the existing local-time → ISO → stored-Date flow intact. Confidence: 0.75
- Explicit quality bar for anything realtime: no duplicate items, no accumulating stale artifacts, no memory leaks, no duplicate socket listeners, no excessive re-renders. Confidence: 0.85
- Do not add a heavy new library when the existing dependency (e.g. Leaflet) can do the job, including animation. Confidence: 0.85
- Use existing project assets; do not download random external images. Confidence: 0.85
- Prefers a single clean visual per entity and sensibly sized/responsive components with correct anchoring, rather than oversized or clustered artwork. Confidence: 0.8
- Keep the existing UI/design and layout; do not redesign screens, controls, or pickers unless required by the fix. When adding a capability to an existing control (e.g. making a value-typeable field), preserve its original geometry exactly — same width, position/alignment, spacing and font as before the change, so neighboring fields keep their sizes too. Confidence: 0.9
- Editable inputs should be forgiving: numeric-only, bounded length/range, preserve leading zeros, mobile numeric keyboard, and never rewrite a value while the user is still typing it. Confidence: 0.8
- Keep verification focused on the changed area; do not run huge full-project audits or every test suite unless the change fails. Confidence: 0.9
- Run runtime/E2E verification against isolated ports and a throwaway database, leaving the user's own running dev servers (backend/frontend) untouched, and remove all temporary artifacts afterwards. Confidence: 0.95
- Provide focused, numbered test results as evidence that the requested scenarios actually pass. Confidence: 0.85
- Wants a short final report covering only the specific points requested (root cause, fix approach, files changed, focused test results) — no long narrative. Confidence: 0.9
- When told something regressed/disappeared, inspect git history/diff to find exactly what was removed before touching code, but do not blindly revert to the old implementation — restore the useful behavior while fixing the defect that made it bad. Confidence: 0.8
- Placeholder/simulated visuals are acceptable only for a stage where no authoritative data exists; they must be clearly separated from real state (smaller/distinct styling), and torn down the moment real data arrives so they can never be mistaken for it. Confidence: 0.8
- New ways to set a value must be additive, not replacing: adding manual/typed entry must keep the existing quick-select dropdown or cards intact. Confidence: 0.75
