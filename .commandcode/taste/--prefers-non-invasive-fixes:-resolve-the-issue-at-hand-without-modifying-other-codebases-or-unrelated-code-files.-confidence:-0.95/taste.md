# - Prefers non-invasive fixes: resolve the issue at hand without modifying other codebases or unrelated code files. Confidence: 0.95
- Prefers non-invasive fixes: resolve the issue at hand without modifying other codebases or unrelated code files. Confidence: 0.95
- Inspects the existing codebase thoroughly before making changes; refuses to assume filenames, routes, components, or libraries. Confidence: 0.9
- When a source file unexpectedly disappears from disk (e.g. `RideHistory.jsx` deleted), recovers it from git before anything else — restores the committed version, then exhaustively searches git stash, reflog, `git fsck` dangling commits/blobs, and editor local history for newer uncommitted work before rebuilding; uncommitted work that only existed on disk is acknowledged as unrecoverable and rebuilt from scratch rather than silently lost. Confidence: 0.75
- Strongly prefers reusing existing components, routes, navigation, and icon libraries rather than creating duplicates or rewriting the app. Confidence: 0.9
- Enforces strict frontend/backend separation: never modifies backend, DB, APIs, controllers, models, or auth/JWT unless a task explicitly requires it; user has explicitly instructed "only work frontend." Confidence: 0.95
- The "only work frontend" rule is a default, not an absolute ban: when the user explicitly asks for a backend/database operation (e.g. "remove all user in use app" → wipe all users to test signup fresh), they expect it to be done directly against the real DB (read connection config, run the query) rather than declined on frontend-only grounds. Confidence: 0.65
- Avoids installing unnecessary or duplicate dependencies (e.g., a new icon package when one already exists). Confidence: 0.85
- Does not modify global CSS or global colors; keeps style changes scoped to the feature at hand. Confidence: 0.85
- Verifies work with lint + production build (and no console errors / no horizontal overflow) before considering a task done. Confidence: 0.85
- Designs mobile-first with responsive checks across phone sizes and no horizontal overflow. Confidence: 0.85
- Falls back to local demo data when a backend/API is unreachable so the UI remains functional and testable. Confidence: 0.85
- Prefers robust, row-based/flex alignment (e.g. independent rows sharing a left column) over fragile gap-based sizing hacks; layout must stay correct when text wraps or grows. Confidence: 0.75
- Keeps vertically stacked UI sections (filter tabs, search bar, stat cards) sharing identical horizontal padding/margins so they line up flush on both edges; negative-margin tricks that push one row past the content edge are treated as alignment bugs to fix. Confidence: 0.75
- Matches UI designs precisely from reference images: uses vision analysis to extract exact specs (shape, color, text weight, separators, spacing, alignment), identifies discrepancies with current implementation, and makes surgical edits until it matches. Confidence: 0.97
- Validates frontend field names against the backend schema (e.g. greps models/ for the field name) rather than guessing, so real data fields are used. Confidence: 0.8
- Starts dev servers only for smoke-testing then stops them; does not leave background processes (dev servers, etc.) running after verification. Confidence: 0.8
- Asks for clarification when a user instruction is ambiguous or a typo (e.g. "start implimat") rather than guessing at intent; confirms the next step before acting. Confidence: 0.7
- Flags destructive/irreversible data operations (e.g. wiping all DB users) with an explicit "destructive and cannot be undone" warning and presents scope options before executing. Confidence: 0.6
- Uses the app's existing design-system/theme tokens — verifying they exist in config (e.g. reading tailwind.config.js) — and maps reference colors to the closest available token instead of hardcoding arbitrary hex values. Confidence: 0.8
- When changing shared styling or helper fields, greps for other usages and applies the same change consistently across related UI surfaces (e.g., card and its detail modal) so they stay in sync. Confidence: 0.75
- Wants a structured final report after implementation: files created/modified, explicit "Backend/API/DB/auth changes: NONE", routes changed, components reused, and test status. Confidence: 0.7
- Prefers compact, dense layouts over oversized ones: explicitly targets approximate pixel heights for cards (e.g. ~110–125px on mobile) and rejects excessive empty space / oversized elements. Confidence: 0.7
- All filter/tab variants of a list must share one identical card/layout — only the ride data and status badge differ; no per-filter layout divergence. Confidence: 0.75
- Removes the browser's default focus outline (`focus:outline-none`) on custom-styled interactive elements (e.g. selected filter pill) so default blue focus rings don't clash with the app's custom theme. Confidence: 0.7
- Prefers using user-supplied image assets placed in `src/assets` (e.g. custom vehicle PNGs) in the UI over generic icon-library glyphs; when told "use this image," inspects the assets folder to find and wire them in, renaming filenames with spaces/special chars to clean importable names. The user repeatedly swaps in their own vehicle image files (bike/car/auto screenshots) to replace the current UI images, and those supplied assets are used as-is — processed/centered, exact colors preserved (e.g. a newly supplied purple bike badge overrides the previously established black/yellow bike styling). Confidence: 0.75
- Prefers logo/icon assets to be clean perfect geometric shapes that match their display container — a square container demands a perfect 1:1 square logo with straight square edges (no circle, no circular crop, no round background), a round container demands a perfect circle; never oval, off-shape, or mismatched. Logos must be full-bleed: the background color fills the entire canvas edge-to-edge (scale-to-cover, not fit) with no unnecessary empty space, so no dark/transparent ring or gap shows around them — if the source has a dark ring inside the image edges (e.g. a circle drawn on a black canvas), flood-fill it with the badge's own color rather than merely scaling up. The display container must match the asset's shape too: showing a circular logo inside a rounded-square/rectangular container (e.g. `rounded-lg`) leaves visible square corners that the user rejects ("outer side is square... remove this square and perfect fit this image in circle") — the container itself must be a perfect circle (`rounded-full` + `overflow-hidden`) with the image filling it edge-to-edge (`h-full w-full object-cover`) so no square corners or dark margins appear around the circle. Symbol precisely centered, maximized to fill the canvas while keeping it fully visible, proportionally scaled with no stretch/squeeze/cropping of important parts, and a minimal professional look with no added text, shadows, or decorative elements; when adapting a logo, the original symbol's identity and colors must be preserved — never redesigned into a different concept. Alpha-channel checks alone are not sufficient — verify visually that no visible boundary/gap remains. For this app, the vehicle badge logos must stay circular: the user explicitly rejected a square full-bleed conversion ("this is not good") and asked to revert to circles, so don't unilaterally change an established logo format — when the user dislikes a format change, promptly revert the previous change rather than defending it. Confidence: 0.9
- Vehicle/ride-card icon style follows the user's reference design: white vehicle silhouettes on solid colored circular badges (auto/bike = yellow, car = blue), rebuilt from the user's original screenshots when the generated asset files disappear; terse instructions like "same like this" plus a reference screenshot mean replicate the reference's exact styling, not just approximate it. Explicit per-logo corrections override the reference style, applied logo-by-logo on demand: "auto logo make it black" made the auto badge a black rickshaw silhouette on yellow, and later "make bike image black" converted the bike silhouette to black as well — the user re-specifies each logo's color with a terse instruction and expects it applied immediately (editing the PNG asset's pixels in place), without defending the earlier reference-based choice. Confidence: 0.85
- When a terse user instruction directly overrides a previously reference-matched design choice (e.g. "auto logo make it black"), apply it immediately without defending the earlier reference-based decision or re-litigating the reference — the user's explicit word is the spec. Confidence: 0.75
- Ride-card details should match the reference design: a dashed vertical route line connecting the green pickup dot to the red destination dot, and the "Completed" status rendered as a subtle outline pill (border + colored text) rather than a solid fill. Confidence: 0.7
- Handles filesystem quirks like Unicode narrow no-break spaces (U+202F) in user-supplied screenshot filenames by glob-matching instead of hardcoding exact paths. Confidence: 0.65
- Handles filesystem quirks like Unicode narrow no-break spaces (U+202F) in user-supplied screenshot filenames by glob-matching instead of hardcoding exact paths. Confidence: 0.65
- Acts immediately on urgent terse requests (e.g. "fix this now") even when the message is full of typos ("serching button", "calender option", "open all years and mothes", "loacllay aslo db") — decodes the obvious intent and implements directly instead of asking for clarification; the user wants prompt fixes when the intent is clear (or the scope was already confirmed in a prior message; e.g. "Invalid credentials fix this" after a remediation was offered). Confidence: 0.92
- Treats every visible UI control as a functional promise: a control that doesn't actually do its advertised job (e.g. a calendar icon that only reset the filter instead of filtering by date) is reported as broken ("cant work") and must be made genuinely functional. Confidence: 0.65
- Removes redundant/duplicate UI controls (e.g. a header search button duplicating the search bar already present below) for a minimal, clean interface. Confidence: 0.6
- Prefers real, fully interactive widgets over simplified approximations: a calendar control must open a true month-grid calendar (month/year title, prev/next navigation, weekday header, tap any day) where the user can select ANY date — including dates that have no rides — not a limited pill-list of only the dates that exist in the data; "open real calender and user enter any date in this calender" means a genuine pick-any-date widget, and filtering works even for dates without rides (empty result is fine). Confidence: 0.7
- Keeps the brand color constant across light and dark themes: the yellow/gold brand accent (buttons, active tabs, accent lines) must stay the same in both themes — theme switching only changes neutral surfaces, text, cards, inputs, and borders, never the brand color. Confidence: 0.85
- Never hardcodes personal or transactional data (name, email, phone, rating, wallet balance, ride history, fares, pickup/destination, dates, upcoming rides) in the UI; uses real app state/backend data, with fallback values only where the existing app explicitly requires them. Confidence: 0.8
- Never fabricates/mocks functionality that already exists, and never fakes a successful flow (e.g. Google/Apple sign-in, auth) that isn't really implemented; uses the real implementation or leaves the control as an honest notice rather than pretending it works. Confidence: 0.75
- Never exposes internal localization keys, variable names, or field names in the UI (e.g. `ride_history_sub`, `create_password`); always renders proper user-facing labels, and wires new UI strings through the project's existing localization system rather than hardcoding them. Confidence: 0.8
- Prefers a centralized, token-based theme system (background/surface/card/textPrimary/textSecondary/border/brand/success/error/info) whose values switch between light and dark, over scattering hardcoded colors; theme changes apply instantly without restart/reload and persist across app restarts using the project's existing storage/preferences mechanism. Confidence: 0.75
- Prefers frontend data adapters/mappers for mapping API responses to the UI rather than modifying backend contracts, endpoints, or response shapes to fit the frontend. Confidence: 0.7
- When a runtime error is reported (e.g. "Failed to fetch dynamically imported module"), first verifies the referenced source file/module is intact and checks for running dev-server processes before assuming a code problem; distinguishes transient Vite dev-server errors (stale module refs after a restart, dependency re-optimization mid-session) from real code bugs, and resolves them with a restart + hard refresh + clearing `node_modules/.vite` rather than editing code. Confidence: 0.7
- The user previews/verifies frontend changes by opening the app in the browser against a live dev server (localhost) — a plain "localhost refused to connect / ERR_CONNECTION_REFUSED" report means the server isn't running, and a passing production build is not a substitute for a live preview; after delivering UI changes, ensure the dev server is running (start it in the background) so they can refresh and check, and offer to stop it once they're done testing. Confidence: 0.6
- Wants debug/test conveniences visible while manually driving real end-to-end flows during development/testing — e.g. "add tamprory opt in otp section" means show a "Temporary OTP" banner in every OTP entry screen (signup and forgot-password alike) whenever the backend returns one, not gated behind frontend dev-only build flags; makes it tappable to auto-fill the field, with production exposure left to backend flags (e.g. OTP_DEBUG / NODE_ENV) so the auth flow stays fully testable manually without leaking OTPs to production. Confidence: 0.6
- New features must be real end-to-end implementations wired to the actual backend (explicitly "do NOT create a fake frontend-only" flow) and verified end-to-end — including negative cases (after a password change: new password logs in, old password no longer works, wrong current password rejected, unauthenticated request 401) — before being reported as done; "do not give a final response until the application builds/runs successfully and the flow has been verified." Confidence: 0.75
- Auth/security-sensitive features (e.g. change password) must follow the project's existing auth architecture: the acting user comes from the authenticated JWT/session (never an arbitrary client-supplied user id — prevents changing another user's password), the current password is verified server-side against the stored hash, the new password is hashed with the project's existing mechanism, and passwords are never stored/logged in the frontend (localStorage, sessionStorage, URLs, console, logs) or sent to unrelated APIs. Confidence: 0.7
- When removing or restructuring fields on one screen (e.g. drop Home/Work address from Personal Information), grep for other consumers of that shared data first and keep the functionality working everywhere else it's used — removal is scoped to the intended surface only, never applied globally if ride-booking or other flows depend on it. Confidence: 0.7
- Detail/profile UI must render the authenticated user's real data; missing fields get a clean fallback (em dash / "Not available") — never undefined, null, or raw API objects — and UI must not invent rows for fields the backend schema doesn't have (e.g. no fabricated username; show it only if the account actually has one). Confidence: 0.65
- User-facing error copy must be clean and curated (e.g. "Current password is incorrect.", "Unable to change password right now. Please try again.") — never reveal raw technical backend errors to the end user. Confidence: 0.6
- Async form/submit UX must be handled properly: disable the submit button and show a loading label while the request is in flight (prevent duplicate submissions), clear sensitive fields after success, and avoid unexpected redirects after completion. Confidence: 0.6
- Reusable controls embedded in list/settings rows must not hardcode a full-width class (e.g. `w-full` baked into a segmented Light/Dark theme toggle) that overrides the width the caller passes in — a baked-in `w-full` stretches the control across the entire row, truncates the sibling row label to an ellipsis ("Ar…"), and makes the control look like a floating overlay; instead let the caller's explicit width win (e.g. `w-[168px]`), compact the control (smaller text/tighter padding) so it sits neatly at the row's right edge with the label fully readable, and verify the row renders correctly in both light and dark themes since such layout bugs are usually theme-independent. Confidence: 0.65
- Explicit task boundaries in a message are the scope spec: when the user says change X and Y and "dont change other parts" (e.g. fix an Appearance toggle + add a language row + black/white login/registration/OTP backgrounds), the requested changes are applied only to those specific surfaces and screens listed, leaving adjacent screens/components (e.g. the Welcome page) and unrelated app parts untouched; reports which screens were deliberately left alone. Confidence: 0.8
- Theme-aware background artwork must cover every screen in the flow, not just the entry screen: when the user asks for image backgrounds on login/create-account/OTP pages (light-theme art vs dark-theme art), the same theme-switching artwork must also show behind sliding/overlay panels — overlays are made translucent with a readability scrim instead of opaque surfaces that would hide the image and create a color seam; the artwork swaps live with the theme selector, so the mapping of which asset is light vs dark is verified (vision/brightness sampling) rather than assumed. Confidence: 0.7
- Treat the user's newest visual directive as the spec even when it reverses an earlier one: after previously requesting flat black/white auth backgrounds, the user later asked to put the theme artwork back on those same login/registration/OTP screens ("add image in background ... and work and convert properly for use select theme") — implement the latest request and promptly undo the prior change rather than defending the earlier state. Confidence: 0.65
- Diagnoses functional bugs (e.g. login showing "Invalid credentials") by tracing the full data path across frontend and backend — reading the form, controller, model, and validators — then reproducing the failure end-to-end against the live API/DB (curl a throwaway signup, inspect the stored record, attempt login) before committing to a fix; test accounts created during verification are deleted afterwards. Confidence: 0.8
- The frontend-only / "don't change other code" default yields when a reported functional defect's root cause is genuinely backend-side: when signup never persists the typed password so every later password login fails, the fix necessarily spans the registration form → OTP verify payload → backend verify-otp controller; "fix this now, don't change other code" means scope the change to the broken flow across both stacks — not "frontend only" and not unrelated files. Confidence: 0.75
- After a backend code change, restarts the running backend so the fix is live and re-verifies end-to-end; restarting a process the user launched is done transparently, and the server is re-started with the environment it originally ran under (e.g. NODE_ENV=development) since an exported shell NODE_ENV overrides the .env value loaded by dotenv. Confidence: 0.65
- Does not unilaterally rewrite a real user's existing account data/credentials to remediate a bug without at least an implicit go-ahead — first offers remediation options (e.g. reset password via OTP / set it directly / delete account) and lets the user choose which path to take for their pre-existing broken account. But when the user replies to that offer with a terse imperative reporting the problem still persists (e.g. "Invalid credentials fix this" after being told how to self-reset), that reply IS the go-ahead for the direct path: the agent is expected to perform the remediation itself on the real account (send OTP → verify with a chosen password → confirm login end-to-end) and hand over the working credentials (email + new password) in the reply, rather than re-explaining the self-service steps or concluding "the fix is functional, please try again." Confidence: 0.65
- When adding a new user-facing string to the UI (e.g. a reset-password hint), adds translations to every supported language dictionary in the project's i18n file (English/Hindi/Marathi), keeping new keys in sync across all languages rather than adding to just one. Confidence: 0.6
- When the user asks to rename or reword a screen/nav label (e.g. "change name profile to account"), the rename means changing the user-visible label value in every supported language's i18n dictionary (en/hi/mr: "Account" / "खाता" / "खाते") — the internal i18n key, route path, and file/component names stay unchanged so no other references need touching. Confidence: 0.6
- Wants debug/test conveniences visible while manually driving real end-to-end flows during development/testing — e.g. "add tamprory opt in otp section" means show a "Temporary OTP" banner in every OTP entry screen (signup and forgot-password alike) whenever the backend returns one, not gated behind frontend dev-only build flags; makes it tappable to auto-fill the field, with production exposure left to backend flags (e.g. OTP_DEBUG / NODE_ENV) so the auth flow stays fully testable manually without leaking OTPs to production. Confidence: 0.6
