# This file is not loaded automatically. Node processes use package-local env:
#
#   backend/.env    — API (MONGO_URI, JWT_SECRET, PORT, CORS_ORIGINS, …)
#   frontend/.env   — Vite (VITE_BASE_URL, …)
#
# Copy backend/.env.example → backend/.env and frontend/.env.example → frontend/.env
# Details: docs/ENVIRONMENT.md
